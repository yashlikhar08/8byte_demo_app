pipeline {
    agent any
    stages {
        stage('BUILD') {
            steps{ 
                echo "BUILDING"
            }
        }

        stage('TEST') {
            steps{ 
                echo "BUILDING"
            }
        }

        stage('SCAN') {
            steps{ 
                echo "BUILDING"
            }
        }

        stage('UPLOAD') {
            steps{ 
                echo "BUILDING"
            }
        }
    }
}